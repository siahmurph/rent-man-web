# .streamlit/config.toml

[theme]
primaryColor = "#1f77b4" # Corporate Blue
backgroundColor = "#ffffff"
secondaryBackgroundColor = "#f0f2f6"
textColor = "#31333F"
font = "sans serif"

[server]
headless = true
port = 8501
enableCORS = false

[client]
showErrorDetails = false # Keeps the UI clean for users
toolbarMode = "minimal"